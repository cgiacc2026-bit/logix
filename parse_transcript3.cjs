const fs = require('fs');
// Wait, the agent context has the JSON in the prompt. I can just output it as a file directly here since I have the text in my memory. But wait, it's 162 invoices, very large. I can just instruct the user to copy-paste it into the UI.
